<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        The window behind the Compose tree. It was hardcoded light with a white
        background, which put a white flash in front of a dark app at every
        cold start. values-night/ overrides the parent and the colour.

        The framework's DayNight parent is API 29+ and minSdk is 26, so the
        switch is done with a resource qualifier instead, which works on all.
    -->
    <style name="Theme.Libra" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>
