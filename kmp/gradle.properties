org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
# Verified 2026-09-21 against :core:jvmTest, :androidApp:assembleDebug and
# :core:iosSimulatorArm64Test — all three store an entry. If a plugin ever
# breaks serialisation, set this to false and name the failing task here.
org.gradle.configuration-cache=true

kotlin.code.style=official
kotlin.native.ignoreDisabledTargets=true

android.useAndroidX=true
android.nonTransitiveRClass=true
